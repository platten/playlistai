# Go
/playlist-ai
/playlistai
*.exe
*.test
*.out
coverage.txt

# Wails v3 / Taskfile
.task
/bin

#Pickles
/pickles

# Frontend
frontend/dist
frontend/node_modules
# generated by `wails3 generate bindings`
frontend/bindings

# Generated packaging artifacts
build/linux/appimage/build
# staged into build/linux/appimage/ by `wails3 task linux:create:appimage`
build/linux/appimage/playlist-ai
build/linux/appimage/playlist-ai.png
# generated by `wails3 generate .desktop`
build/linux/playlist-ai.desktop
build/windows/nsis/MicrosoftEdgeWebview2Setup.exe

# Python tooling
__pycache__/
*.pyc
.venv/

# Downloaded assets — never committed; fetched on first launch
/models/*.gguf
/models/*.onnx
/models/*.bin
/models/*.sqlite
# the converted catalog and its compressed archive — never committed (fixtures
# under internal/**/testdata stay tracked). The archive is hosted off-repo and
# the app downloads it on first launch; see docs/CATALOG.md.
/build/catalog/
/build/catalog-dist/

# Local dev
*.log
.DS_Store
.env
.env.local
/playlist-ai-data/

# Editor / IDE
.idea/
.vscode/
*.swp
